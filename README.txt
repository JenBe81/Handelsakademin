I have worked in Windows environment. And to make sure you get the same enviornmental setup make a new virtual enviornment and install the same packades as I have used by running these commands:

python -m venv new_venv
new_venv/Scripts/activate
pip install -r requirements.txt
